const pool = require('./config/database'); async function check() { const client = await pool.connect(); try { const result = await client.query('SELECT table_name FROM information_schema.tables WHERE table_schema = \\'public\\' ORDER BY table_name'); console.log('Tables in the database:'); result.rows.forEach(row => console.log(row.table_name)); } finally { client.release(); } } check().then(() => process.exit(0)).catch(err => { console.error(err); process.exit(1); });
